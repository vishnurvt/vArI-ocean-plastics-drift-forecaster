# Copy to `.env` and adjust values for your machine.
# Load it with `uv run --env-file .env` or via tools like `direnv`.

# Force UTF-8 output on Windows terminals.
PYTHONUTF8=1

# Ensure the local package is importable when running scripts without installation.
PYTHONPATH=.

# Use a non-interactive Matplotlib backend for headless rendering.
MPLBACKEND=Agg

# Optional: prepend a custom FFmpeg location to PATH (uncomment and edit).
#PATH=C:\tools\ffmpeg\bin;${PATH}

# Optional: default Dask scheduler for parameter sweeps (leave blank to run locally).
DASK_SCHEDULER_ADDRESS=

# Optional: cap the number of threads numba uses during simulations.
NUMBA_NUM_THREADS=
